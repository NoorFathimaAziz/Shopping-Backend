commands -

1. create gitignore and readme,md
2. initialize venv
    - pip -m venv venv
3. requirements    
    - pip freeze > requirements.txt
    - pip install -r "requirements.txt"
4. request 
    - flask ofer request module to acess the request data or headers

5. jsonify
    - it converts the dict/list to json 